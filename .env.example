HF_TOKEN=Your_token

# 0 = default: download model once if missing, then reuse local cache
# 1 = offline: load from cache only, zero network calls (fails if not cached yet)
# Accepts: 1/0, true/false, yes/no (case-insensitive)
OFFLINE_MODE=0
